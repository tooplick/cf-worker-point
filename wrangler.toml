# CF Worker Trojan 代理节点 — 部署配置
name = "cf-worker-point"
main = "_worker.js"
compatibility_date = "2024-01-01"

# 环境变量（可选，覆盖脚本默认值）
# [vars]
# proxyip = "youxuan.cf.090227.xyz"

[observability]
[observability.logs]
enabled = true
invocation_logs = true
